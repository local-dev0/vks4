<include>
  <context name="public">
    <!-- Принимаем входящий SIP-звонок. Любой destination_number отправляется bridge'ом
         к sip-gateway B2BUA. Реальный room_id передаётся через SIP To-URI user-part
         (например sip:room-XXX@host) — sip-gateway его парсит. -->
    <extension name="vks_inbound">
      <condition field="destination_number" expression="^(.*)$">
        <action application="set" data="hangup_after_bridge=true"/>
        <action application="set" data="ringback=silence_stream://-1"/>
        <action application="set" data="continue_on_fail=false"/>
        <!-- Inbound leg негоциирует свободно с Polycom (PCMU/G.722/etc). Codec="opus"
             ставится только на OUTBOUND leg через {var=value} prefix в bridge data — FS делает
             transcoding inbound→opus. -->
        <action application="set" data="bypass_media=false"/>
        <action application="set" data="video_possible=true"/>
        <!-- Раздельные prefs: audio_codec_prefs форсит Opus, video_codec_prefs форсит H264.
             absolute_codec_string смешивает их и ломает negotiation (FS либо игнорит видео,
             либо сваливается на inbound PCMU). -->
        <action application="set" data="audio_codec_prefs=opus"/>
        <action application="set" data="video_codec_prefs=H264"/>
        <action application="log" data="INFO Incoming SIP call: from=${caller_id_number} to=${destination_number}"/>
        <!-- НЕТ ни answer, ни pre_answer — bridge сам ответит после установки outbound leg.
             Это даёт FS свободу negotiate Opus на outbound и H264 на video одновременно. -->
        <action application="bridge"
          data="{originate_video=true,video_possible=true,audio_codec_prefs=opus,video_codec_prefs=H264,sip_h_X-VKS-Room=${destination_number},sip_h_X-VKS-Caller=${caller_id_number}}sofia/gateway/vks-sipgw/${destination_number}"/>
      </condition>
    </extension>
  </context>
</include>
